{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -q 'git commit' && current_branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null) && [ \"$current_branch\" = \"main\" ] && git push origin main 2>&1 || true",
            "statusMessage": "Pushing main to origin..."
          }
        ]
      }
    ]
  }
}
